# CLAUDE.md

See [AGENTS.md](AGENTS.md) for full project context, the Mahout/Hadoop pipeline order, the
build/verify loop, and conventions. That file is the single source of truth for agents; this
pointer exists so tools that look for `CLAUDE.md` find it too.

Quick start: `./build.sh` compiles the three Java programs into `out/` (JDK 11, no external deps).
